# Copy to .streamlit/secrets.toml (gitignored) and fill in real values.
# Leave both blank to run the dashboard in demo-data mode.
#
# NOTE: app.py reads SUPABASE_KEY, not SUPABASE_SERVICE_ROLE_KEY (the name
# used in the backend's .env). Use the project's anon/read-only key here —
# this dashboard only needs SELECT access to claim_diagnostics, and the
# service-role key should stay confined to the backend writer.

SUPABASE_URL = "https://uurrjcsvdtprnpfqpver.supabase.co"
SUPABASE_KEY = ""
